let sampleArray = [ "this", "array", "contains", "script", "word"]
console.log(sampleArray.lastIndexOf("script"));

const sampleString = 'thi string contains scriptword';
const searchTerm = "scrip"
console.log(sampleString.lastIndexOf(searchTerm));

